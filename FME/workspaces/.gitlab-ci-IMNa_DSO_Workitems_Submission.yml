stages:           # List of stages for jobs, and their order of execution
  - build
  
build-dev-dso-submit:        # This job runs in the build stage, which runs first.
  stage: build

  cache: null

  script:
    - echo "BEGIN - Deploying the code for dev..."
    - if (Test-Path $IMNa_PATH\FME) { Remove-Item $IMNa_PATH\FME -Force -Recurse }
    - cp -R FME $IMNa_PATH\FME
    - Invoke-Command -ScriptBlock { D:\GitLab-Deployments\IMNa\FME\scripts\IMNa_DSO_Workitems_Submission.ps1 D:\GitLab-Deployments\IMNa\FME\properties\dev.properties $FME_TOKEN_NEW_DEV}
    - echo "END - Deployment complete."

  environment:
    name: development

  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH 
      changes:
        - FME/projects/IMNa_DSO_Workitems_Submission.fsproject
        - FME/properties/IMNa_DSO_Workitems_Submission_Dev.xml
        - FME/scripts/IMNa_DSO_Workitems_Submission.ps1
        - FME/xslts/IMNa_DSO_Workitems_Submission.xslt
        - FME/workspaces/IMNa_DSO/*
        - FME/workspaces/.gitlab-ci-IMNa_DSO_Workitems_Submission.yml
      when: manual

build-test-dso-submit:        # This job runs in the build stage, which runs first.
  stage: build

  cache: null

  script:
    - echo "BEGIN - Deploying the code for test..."
    - if (Test-Path $IMNa_PATH\FME) { Remove-Item $IMNa_PATH\FME -Force -Recurse }
    - cp -R FME $IMNa_PATH\FME
    - Invoke-Command -ScriptBlock { D:\GitLab-Deployments\IMNa\FME\scripts\IMNa_DSO_Workitems_Submission.ps1 D:\GitLab-Deployments\IMNa\FME\properties\test.properties $FME_TOKEN_NEW_TEST}
    - echo "END - Deployment complete."

  environment:
    name: test

  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      changes:
        - FME/projects/IMNa_DSO_Workitems_Submission.fsproject
        - FME/properties/IMNa_DSO_Workitems_Submission_Test.xml
        - FME/scripts/IMNa_DSO_Workitems_Submission.ps1
        - FME/xslts/IMNa_DSO_Workitems_Submission.xslt
        - FME/workspaces/IMNa_DSO/*
        - FME/workspaces/.gitlab-ci-IMNa_DSO_Workitems_Submission.yml
      when: manual

build-acc-dso-submit:        # This job runs in the build stage, which runs first.
  stage: build

  cache: null

  script:
    - echo "BEGIN - Deploying the code for acc..."
    - if (Test-Path $IMNa_PATH\FME) { Remove-Item $IMNa_PATH\FME -Force -Recurse }
    - cp -R FME $IMNa_PATH\FME
    - Invoke-Command -ScriptBlock { D:\GitLab-Deployments\IMNa\FME\scripts\IMNa_DSO_Workitems_Submission.ps1 D:\GitLab-Deployments\IMNa\FME\properties\acc.properties $FME_TOKEN_NEW_ACC}
    - echo "END - Deployment complete."

  environment:
    name: acceptance
    
  rules:
    - if: '$CI_COMMIT_TAG =~ /^v\d+.\d+.\d+-DSO$/'
      when: manual


build-prod-dso-submit:        # This job runs in the build stage, which runs first.
  stage: build

  cache: null

  script:
    - echo "BEGIN - Deploying the code for prod..."
    - if (Test-Path $IMNa_PATH\FME) { Remove-Item $IMNa_PATH\FME -Force -Recurse }
    - cp -R FME $IMNa_PATH\FME
    - Invoke-Command -ScriptBlock { D:\GitLab-Deployments\IMNa\FME\scripts\IMNa_DSO_Workitems_Submission.ps1 D:\GitLab-Deployments\IMNa\FME\properties\prod.properties $FME_TOKEN_NEW_PROD}
    - echo "END - Deployment complete."

  environment:
    name: production
    
  rules:
    - if: '$CI_COMMIT_TAG =~ /^v\d+.\d+.\d+-DSO$/'
      when: manual